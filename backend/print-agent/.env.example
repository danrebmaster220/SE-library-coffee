# Print Agent Environment Configuration
# Copy this to .env and update values

# Cloud Backend URL (your Render deployment)
PRINT_AGENT_SERVER=https://your-app-name.onrender.com

# Printer location identifier
PRINTER_LOCATION=main-counter

# Windows printer name (for fallback printing)
PRINTER_NAME=POS-58
